[flake8]

ignore = E501, W503, E712, F541, E402

exclude =
    # No need to traverse our git directory
    .git,
    # There's no value in checking cache directories
    __pycache__,
    venv,
    ./zadalaAPI/settings.py,
    ./authentication/migrations/*